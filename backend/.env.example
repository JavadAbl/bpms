PORT=3001
JWT_SECRET=bpms-dev-secret-change-in-production
# SQLite database file. Prefer an absolute path to avoid resolution surprises,
# e.g. file:/absolute/path/to/bpms-backend/db/bpms.db
DATABASE_URL="file:./db/bpms.db"
